import { SuperAdminPanel } from "@/components/SuperAdminPanel";

export const metadata = {
  title: "Memoora Yönetim Paneli",
  robots: "noindex",
};

export default function SuperAdminPage() {
  return <SuperAdminPanel />;
}
